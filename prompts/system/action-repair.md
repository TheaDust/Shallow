# 行动：根据外部黑盒观察修复当前工作包

下面的报告来自独立浏览器验收。它描述可观察现象，不是完整测试集，不是新的需求，也不代表已经确定了代码根因。

## 已通过的观察

{{PASSED_CASE_IDS}}

## 失败观察

{{FAILURES}}

修复要求：

1. 先检查当前实现以及与失败现象相关的界面、请求、服务端处理和状态读取路径；读取项目根的 `ARCHITECTURE.md`（如存在）；其中的笔记仅供定位和理解项目，需求与平台合同优先，当前实现以代码核对。
2. 用最低成本复现或验证失败现象。
3. 定位产生现象的责任层，修复共同原因，不要针对错误消息或用例输入增加特例。
4. 保留已通过观察对应的行为以及其他已存在功能。
5. 在最终构建和浏览器自测前，按需更新项目根的 `ARCHITECTURE.md`：仅在稳定事实新增、变化或过时时修改，不存在且有值得保留的事实时才创建。记录模块边界、关键入口路径、数据模型约定、技术选型理由，以及代码或可复现检查已确认的注意事项；直接修订或删除过时条目，合并重复内容；全文通常控制在约 60 行，保留必要约定时可适当超出，精简时优先删除过时和重复内容。使用一级标题 `# 项目架构笔记`，按顺序组织为 `## 技术选型与理由`、`## 模块与入口`、`## 数据与接口约定`、`## 已确认的注意事项`；空小节可省略。小节内用简短列表，每条记录一个稳定事实，必要时附关键入口路径或简短理由。省略逐行实现说明、任务流水账和未经确认的推测；没有变化时保持文件不动。
6. 按自测要求调用 `candidate.prepare`，完成相关构建和关键浏览器路径检查，然后输出结果收据。
