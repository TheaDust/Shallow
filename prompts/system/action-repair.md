# 行动：修复已确认的业务失败

当前工作包汇总了独立验收中可复现的业务失败。先按共享组件、接口或数据规则归并根因，再进行一次修复，保留已经存在的行为。

已通过的用例标识：
{{PASSED_CASE_IDS}}

允许使用的失败观测：
{{FAILURES}}

检查相关代码和 ARCHITECTURE.md（如存在）；需求与平台合同优先，当前实现以代码核对。只修改解释这些失败所必需的实现，按需更新稳定接口约定。重复控件应具有合理的可访问性，但不能为消除探针定位歧义随意重命名、隐藏或删除产品控件。
需要浏览器复现时，按自测约定验证一条代表性的失败路径。完成后输出简短回执，独立 Judge 将重新检查修复和既有路径。
