# 行动：执行最后一次根因修复

这是当前工作包的最后一次允许修复。不要立即修改表面症状；先根据代码和可复现证据确定最可能的根因。

## 已通过的观察

{{PASSED_CASE_IDS}}

## 仍然失败的观察

{{FAILURES}}

按与问题有关的部分检查以下因果链：

用户操作
→ 客户端状态
→ HTTP 请求
→ 身份认证和权限判断
→ 领域状态变更
→ 持久化
→ 重新读取
→ 界面渲染

修复要求：

1. 确认失败最早出现在哪一层，以及后续现象是否只是连锁结果；读取项目根的 `ARCHITECTURE.md`（如存在）；其中的笔记仅供定位和理解项目，需求与平台合同优先，当前实现以代码核对。
2. 修复完整因果链上的共同问题，不扩大当前工作包范围。
3. 防止修复破坏已经通过的场景。
4. 在最终构建和浏览器自测前，按需更新项目根的 `ARCHITECTURE.md`：仅在稳定事实新增、变化或过时时修改，不存在且有值得保留的事实时才创建。记录模块边界、关键入口路径、数据模型约定、技术选型理由，以及代码或可复现检查已确认的注意事项；直接修订或删除过时条目，合并重复内容；全文通常控制在约 60 行，保留必要约定时可适当超出，精简时优先删除过时和重复内容。使用一级标题 `# 项目架构笔记`，按顺序组织为 `## 技术选型与理由`、`## 模块与入口`、`## 数据与接口约定`、`## 已确认的注意事项`；空小节可省略。小节内用简短列表，每条记录一个稳定事实，必要时附关键入口路径或简短理由。省略逐行实现说明、任务流水账和未经确认的推测；没有变化时保持文件不动。
5. 按自测要求调用 `candidate.prepare`，完成相关构建和关键浏览器路径检查。
6. 在结果收据中用不超过三句话给出根因摘要；不要输出详细内部思维过程。
